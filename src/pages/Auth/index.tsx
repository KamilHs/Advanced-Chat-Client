import { useTheme } from "@material-ui/core";
import React from "react";
import { LoginForm } from "../../modules";

const Auth: React.FC = () => {
    return (
        <LoginForm />
    );
}

export default Auth;